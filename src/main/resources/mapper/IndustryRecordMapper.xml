<mapper namespace="xyz.fcc.stock.record.mapper.IndustryRecordMapper">
    <insert id="insertIndustryRecord" parameterType="xyz.fcc.stock.record.entity.IndustryRecord">
        INSERT INTO t_industry_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="currentDay != null">current_day,</if>
            <if test="industry != null and industry != ''">industry,</if>
            <if test="content != null">content,</if>
            <if test="type != null">type,</if>
            <if test="info != null">info,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="currentDay != null">#{currentDay},</if>
            <if test="industry != null and industry != ''">#{industry},</if>
            <if test="content != null">#{content},</if>
            <if test="type != null">#{type},</if>
            <if test="info != null">#{info},</if>
        </trim>
        ON DUPLICATE KEY UPDATE
        content = VALUES(content),
        type = VALUES(type),
        info = VALUES(info),
        update_time = CURRENT_TIMESTAMP
    </insert>

    <select id="countIndustryRecordsByCondition" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM t_industry_record
        <where>
            1=1
            <if test="industry != null and industry != ''">
                AND industry = #{industry}
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="startDate != null and endDate != null">
                AND current_day BETWEEN #{startDate} AND #{endDate}
            </if>
            <if test="startDate != null and endDate == null">
                AND current_day <![CDATA[ >= ]]> #{startDate}
            </if>
            <if test="startDate == null and endDate != null">
                AND current_day <![CDATA[ <= ]]> #{endDate}
            </if>
            <if test="content != null and content != ''">
                AND content LIKE CONCAT('%', #{content}, '%')
            </if>
        </where>
    </select>

    <select id="selectIndustryRecordByConditionWithPaging" resultType="xyz.fcc.stock.record.entity.IndustryRecord">
        SELECT
        id,
        current_day AS currentDay,
        industry,
        content,
        type,
        create_time AS createTime,
        update_time AS updateTime,
        info
        FROM t_industry_record
        <where>
            1=1
            <if test="industry != null and industry != ''">
                AND industry = #{industry}
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="startDate != null and endDate != null">
                AND current_day BETWEEN #{startDate} AND #{endDate}
            </if>
            <if test="startDate != null and endDate == null">
                AND current_day <![CDATA[ >= ]]> #{startDate}
            </if>
            <if test="startDate == null and endDate != null">
                AND current_day <![CDATA[ <= ]]> #{endDate}
            </if>
            <if test="content != null and content != ''">
                AND content LIKE CONCAT('%', #{content}, '%')
            </if>
        </where>
        ORDER BY current_day DESC
        LIMIT #{offset}, #{limit}
    </select>
</mapper>